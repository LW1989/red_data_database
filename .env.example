# Database Configuration (for Docker Compose)
# These variables are used by docker-compose.yml to configure the PostgreSQL container
POSTGRES_DB=zensus_db
POSTGRES_USER=zensus_user
POSTGRES_PASSWORD=changeme
POSTGRES_PORT=5432

# ETL Script Configuration
# These variables are used by etl/utils.py to connect to the database
# For local development, these should match the Docker Compose settings above
DB_HOST=localhost
DB_PORT=5432
DB_NAME=zensus_db
DB_USER=zensus_user
DB_PASSWORD=changeme

# Instructions:
# 1. Copy this file to .env: cp .env.example .env
# 2. Edit .env and replace 'changeme' with a strong password
# 3. For server deployment, use different passwords for production
# 4. Never commit .env to version control (it's in .gitignore)
